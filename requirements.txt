# AI Media Translation & Publishing Agent dependencies

# Core Inference / AI Models
torch==2.6.0
torchaudio==2.6.0
faster-whisper==1.0.3        # Stage 2: Fast local ASR
indic-transliteration==2.3.61
pyannote.audio>=3.3.1        # Stage 2: Speaker Diarization
TTS==0.22.0                  # Stage 4: Coqui XTTSv2 Voice Cloning
deep-translator==1.11.4      # Stage 3: Free Translation (Google Translate fallback)
gtts>=2.5.0                  # Stage 4: Fallback TTS for XTTS-unsupported languages (e.g. Kannada)
librosa>=0.10.0              # Stage 4: Pitch/F0 analysis for speaker gender detection
edge-tts>=6.1.0              # Stage 4: Gender-specific neural TTS for Indic languages
nest_asyncio>=1.6.0          # Stage 4: Allows Edge-TTS async to run safely inside Streamlit's event loop

# Audio Processing
pydub==0.25.1                # Stage 5: Audio mixing, EQ
pyloudnorm==0.1.1            # Stage 5: LUFS normalization
soundfile==0.12.1            # Audio file I/O

# Video Processing & Subtitles
ffmpeg-python==0.2.0         # Stages 1, 6-8: Video processing, audio extraction, burn-in
opencv-python==4.9.0.80      # Stage 9: Video properties validation

# Web UI / Application
streamlit==1.32.2            # Simple frontend UI
python-dotenv==1.0.1         # Environment variable management

# Utilities
numpy
scipy
Requests
tqdm
google-generativeai==0.8.3
